<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/et_user_account"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="帳號"
        android:inputType="textPersonName"/>

    <EditText
        android:id="@+id/et_password_account"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_user_account"
        android:ems="10"
        android:hint="驗證碼"
        android:inputType="textPersonName"/>

    <Button
        android:id="@+id/btn_login"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toEndOf="@+id/et_user_account"
        android:layout_above="@id/image_view"
        android:layout_centerVertical="true"
        android:text="送出"/>

    <com.tonynowater.uniforminvoicehelper.view.component.SBarCodeView
        android:id="@+id/image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/et_password_account"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>